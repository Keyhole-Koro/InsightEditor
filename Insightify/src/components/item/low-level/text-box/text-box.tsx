export {}
/*
class TextBox extends BaseItemClass {
    text: string;
    fontSize: number;
  
    constructor({
      text = "Default Text",
      fontSize = 16,
      ...rest
    }: Partial<TextBoxItem> = {}) {
      super(rest);
      this.text = text;
      this.fontSize = fontSize;
    }
  
    render(): JSX.Element {
      return (
        <text x={this.x} y={this.y} fontSize={this.fontSize * this.scale} fill={this.color}>
          {this.text}
        </text>
      );
    }
  }
  */