import React from 'react';
import { BaseItem, BaseItemClass  } from '../../base-item'; // Import Circle interface
import { RGBColor } from '../../../utils/color';
/*
class Square extends BaseItemClass {
    size: number;
  
    constructor({
      size = 40,
      ...rest
    }: Partial<SquareItem> = {}) {
      super(rest);
      this.size = size;
    }
  
    render(): JSX.Element {
      return (
        <rect x={this.x} y={this.y} width={this.size * this.scale} height={this.size * this.scale} fill={this.color} />
      );
    }
  }
  */